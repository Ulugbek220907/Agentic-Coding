# Math Function Plotter

Use `plot_expressions` whenever the user asks to visualize, graph, or plot
one or more mathematical functions of x (e.g. "plot sin(x) + log(x)").
Returns a PNG chart path. Prefer this over describing the shape of a
function in words alone.